#include <iostream>
#include "add.h"
int main() {
    int x = 3, y = 4;
    std::cout << x << " + " << y << " = " << add(x, y) << std::endl;
    std::cout << a << "-" << b << "=" << sub(a, b) << std::endl;  // ĞÂÔö
    return 0;
}